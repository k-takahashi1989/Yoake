# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

platform :ios, min_ios_version_supported
prepare_react_native_project!

# Firebase iOS SDK v11+ (Swift 製) を ObjC から import するために必要。
# gRPC-C++ の modulemap 問題は post_install で CLANG_ENABLE_MODULES = NO により回避する。
use_modular_headers!

target 'Yoake' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath],
    # An absolute path to your application root.
    :app_path => "#{Pod::Config.instance.installation_root}/.."
  )

  post_install do |installer|
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false,
      # :ccache_enabled => true
    )

    # use_modular_headers! グローバル時に gRPC-C++ が依存する gRPC-Core の
    # modulemap が見つからないエラーを防ぐため、gRPC 関連 pod の Clang modules を無効化する
    grpc_pods = %w[gRPC-C++ gRPC-Core BoringSSL-GRPC abseil]
    installer.pods_project.targets.each do |target|
      if grpc_pods.include?(target.name)
        target.build_configurations.each do |build_config|
          build_config.build_settings['CLANG_ENABLE_MODULES'] = 'NO'
        end
      end
    end
  end
end
